{
  "name": "domvault-services",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Constellation registry + Observatory query services (zero runtime deps, zero telemetry)",
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test $(find registry client observatory e2e -name '*.test.ts' -not -path '*/node_modules/*')",
    "registry:dev": "tsx registry/server/server.ts",
    "observatory:dev": "tsx observatory/server/endpoint.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
